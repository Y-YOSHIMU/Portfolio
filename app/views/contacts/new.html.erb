<h1>お問い合わせ</h1>
<%= form_for(@contact) do |f| %>
	<h4>お名前</h4>
	<%= f.text_field :name %>
	<h4>メールアドレス＊任意</h4>
	<%= f.text_field :email %>
	<h4>メッセージ</h4>
	<%= f.text_area :message %>
	<%= f.submit "送信" %>
<% end %>